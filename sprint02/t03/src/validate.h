#pragma once

#include <iostream>
#include <fstream>
#include <ostream>
#include <map>
#include <vector>
#include <regex>
#include <string>
#include <sstream>
#include <iterator>

#define REGEX R"lit(\s*"([\"]+)"\s*:\s*"([\"]+)"\s*)lit"
